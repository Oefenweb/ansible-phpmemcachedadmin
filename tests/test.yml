# test file
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: pre | create user (if needed)
      user:
        name: travis
  roles:
    - ../../
  vars:
    phpmemcachedadmin_install_dirs:
      - dest: /tmp1
        owner: travis
      - dest: /tmp2
        owner: travis
        group: root
      - dest: /tmp3
        state: absent
