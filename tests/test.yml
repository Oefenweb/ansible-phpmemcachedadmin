# test file for phpmemcachedadmin
---
- hosts: localhost
  connection: local
  sudo: true
  roles:
    - ../../
  vars:
    phpmemcachedadmin_install_dirs:
      - dest: /tmp1
        owner: travis
      - dest: /tmp2
        owner: travis
        group: root
      - dest: /tmp3
        state: absent
